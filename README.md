This repository contains a simple C program that exhibits unexpected behavior due to pointer manipulation. The program initializes an integer variable, then modifies it indirectly using a pointer.  The provided solution clarifies the pointer operations and addresses the unexpected behavior. The solution highlights safe pointer handling and memory management.